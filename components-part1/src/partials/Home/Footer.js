import React from 'react';
import Logo from '../../components/Logo';
import { FooterWrapper } from '../../styledComponents/Footer';

const Footer = () => {
  return (
    <FooterWrapper className="footer">
      <Logo />
    </FooterWrapper>
  );
};
export default Footer;
